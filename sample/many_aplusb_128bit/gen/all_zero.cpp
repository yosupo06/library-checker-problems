#include <iostream>
#include <vector>

#include "../params.h"
#include "random.h"
using namespace std;

int main(int, char**) {
    int t = T_MAX;
    printf("%d\n", t);
    for (int i = 0; i < t; i++) {
        printf("0 0\n");
    }
    return 0;
}
